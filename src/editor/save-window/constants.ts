/** Minimum intersection ratio (target area) to keep a partially overlapping operation. */
export const SAVE_WINDOW_INTERSECTION_THRESHOLD = 0.6;

/** Minimum drag distance before a save-window draw is recognized. */
export const SAVE_WINDOW_DRAG_THRESHOLD_PX = 6;

/** Minimum drawn window size in pixels. */
export const SAVE_WINDOW_MIN_RECT_PX = 8;
