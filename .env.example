# Public release defaults (no backend or AI required)
PUBLIC_AGENT_ENABLED=false
PUBLIC_BACKEND_ENABLED=false

# Local developer build only (optional)
# PUBLIC_AGENT_ENABLED=true
# LOCAL_AGENT_SERVER_URL=http://localhost:4000
